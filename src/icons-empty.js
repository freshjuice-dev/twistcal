/**
 * Empty icons — used by slim builds via rollup alias.
 * Drops ~8 KB from the bundle. Icons are simply absent, show-icons has no effect.
 */
export const ICONS = {
  google: '',
  outlook: '',
  yahoo: '',
  ical: '',
};